-- This file is now merged into schema.sql and seed.sql
-- It's kept for reference but no longer needed for setup

-- Teachers table is now created in schema.sql
-- Sample teacher data is now inserted in seed.sql
-- Subject-teacher mappings are now inserted in seed.sql